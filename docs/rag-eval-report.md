# RAG 检索质量评测报告

> 由 `scripts/eval/run-eval.py` 自动生成，语料 `scripts/eval/corpus.md`、查询 `scripts/eval/queries.json`，可完整复跑。

## 1. 评测设置

| 项 | 值 |
|---|---|
| 语料 | 30 个知识点（八股笔记，含具体参数与流程） |
| 切分 | 按 Markdown 标题优先切分，600 字上限、重叠 100 |
| Embedding | Ollama bge-m3，1024 维 |
| 向量库 | PostgreSQL 17 + pgvector 0.8.6，HNSW + cosine |
| 正样本 | 40 条自然口语提问（措辞刻意不与文档标题重合，expect 对应知识点 tag） |
| 负样本 | 20 条语料中完全不存在的主题（用于测假阳性） |
| 命中判定 | 检索结果的 `tags` 字段包含期望 tag，不依赖人工判断 |

## 2. 正样本检索质量

| 指标 | 结果 |
|---|---|
| **Hit@1** | 28/40 = **70.0%** |
| Hit@3 | 38/40 = 95.0% |
| Hit@5 | 38/40 = 95.0% |
| **MRR** | **0.821** |
| Top1 相似度 | 均值 62.5%，区间 40.1%~78.3% |

以下 12 条查询的正确答案**未排在 Top1**（最后一列是它在结果中的实际排名，`>5` 表示未被召回）：

| 期望知识点 | 提问 | 实际 Top1 | 正确排名 |
|---|---|---|---|
| `jvm-gc` | CMS 当年为什么被废弃，它有什么硬伤 | RDB 与 AOF 持久化 | 2 |
| `jvm-classload` | 为什么自己写一个 java.lang.String 不会生效 | AOP 与动态代理 | >5 |
| `jvm-oom` | 服务跑几个小时就崩，怎么保留现场并定位是哪块内存泄漏 | ThreadLocal 与内存泄漏 | 2 |
| `concurrency-lock` | 两个线程抢同一把锁，JVM 内部是怎么处理的，抢不到会直接挂起吗 | JVM 运行时内存区域与参数 | 2 |
| `concurrency-lock` | 偏向锁后来为什么要默认关掉 | 事务隔离级别与 MVCC | 2 |
| `mysql-index-fail` | SQL 里对时间字段用了 YEAR() 函数，索引还能用吗 | 索引结构与 B+ 树 | 2 |
| `mysql-index-fail` | 手机号是字符类型的列，用数字去查会走索引吗 | 索引结构与 B+ 树 | 2 |
| `mysql-mvcc` | 同一个事务里两次查询结果必须一致，这是怎么做到的 | 分布式事务 | >5 |
| `mysql-lock` | update 语句没走索引会锁多少行 | 事务隔离级别与 MVCC | 2 |
| `spring-tx` | 把事务注解放到 private 方法上为什么没用 | AOP 与动态代理 | 2 |
| `dist-tx` | 跨库转账这种场景，怎么保证最终一致而不是用强一致 | 缓存穿透、击穿、雪崩 | 2 |
| `ai-rag` | 召回结果里专有名词和代码标识符老是搜不到，有什么办法补足 | 索引失效场景 | 3 |

## 3. 负样本对照

| 指标 | 值 |
|---|---|
| 负样本最高分（均值） | 53.2% |
| 负样本最高分（区间） | 43.9%~59.9% |
| 正样本 Top1 最低分 | 40.1% |
| **区间重叠** | 负样本最高分比正样本最低分**还高 19.9 个百分点** |

## 4. 阈值敏感性

假设「相似度 ≥ 阈值即认为命中」，统计正样本通过率与负样本误判率：

| 阈值 | 正样本 Top1 通过 | 负样本误判 |
|---|---|---|
| ≥45% | 39/40 (97.5%) | 19/20 (95.0%) |
| ≥50% | 38/40 (95.0%) | 16/20 (80.0%) |
| ≥52% | 37/40 (92.5%) | 12/20 (60.0%) |
| ≥54% | 37/40 (92.5%) | 8/20 (40.0%) |
| ≥56% | 37/40 (92.5%) | 6/20 (30.0%) |
| ≥58% | 33/40 (82.5%) | 3/20 (15.0%) |
| ≥60% | 29/40 (72.5%) | 0/20 (0.0%) |
| ≥65% | 9/40 (22.5%) | 0/20 (0.0%) |
| ≥70% | 4/40 (10.0%) | 0/20 (0.0%) |

## 5. 结论

1. **排序指标可用**：Hit@1 70.0%、Hit@3 95.0%、MRR 0.821，说明向量检索在「自然口语提问 → 知识点」这个任务上排序是有效的。
2. **绝对相似度不可作阈值**：负样本（语料中完全不存在的主题）最高分 59.9%，比正样本的最低分（40.1%）还高 19.9 个百分点，两个区间完全重叠。阈值敏感性分析给出了更明确的结论——要让负样本误判率降到 0，阈值必须提到 60% 以上，但正样本会因此损失约四分之一（通过率跌到 72.5%）；反之阈值定在 55% 时负样本误判率高达 40%。**不存在可用的工作点**，因此这类系统的验收标准应该是排序指标（Hit@K / MRR），而不是「相似度 > 0.7 才算命中」这种写法。
3. **topK = 3 是性价比最高的取值**：12 条查询的正确答案不在 Top1，其中 10 条落在 Top2~Top3；Hit@3 与 Hit@5 完全相同（均 95.0%），说明取 3 条已覆盖全部可召回结果，取 5 条只是徒增送入 LLM 的上下文开销。
4. **提升方向**：剩余错误主要来自主题相近的知识点互相干扰（如同一领域下的两个细分主题），可考虑混合检索（向量 + BM25 关键词）或加一层 Rerank。

---

*评测耗时 1 秒；命中判定基于文档 tags 自动完成，无人工标注介入。*
